cmake_minimum_required(VERSION 2.8)
project( submission )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( submission submission.cpp )
target_link_libraries( submission ${OpenCV_LIBS} )